#define PI 3.1415926535
#if defined(_OPENMP)
#include <omp.h>
#endif
• The preprocessor is executed;
before the compilation.
• Preprocessing directives begin with;
a # character;
• Each directive occupies one line;
• preprocessing instruction;
(define, undef, include, if, ifd
ef, ifndef, else, elif, endif, l
ine, error, pragma);
